# Map source file paths (relative to the site source) to alt text.
# The plugin will also try the basename if the full relative path is not found.

/assets/portfolio/unbound/camera-movement/pitch-to-camera-offset.png: "3 curves, for the x, y and z offset of the camera, that estimate where the camera would be without head pitch given the pitch value."
